import {Schema, model} from 'mongoose';

const ItemModel = new Schema({
  name: {
    type: String,
    required: true,
  },
  description: {
    type: String,
    required: true,
  },
  price: {
    type: Number,
    required: true,
  },
});

export default model('ItemModel', ItemModel);
